<%= form_for(@playlist) do |f| %>
<% if @playlist.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

    <ul>
        <% @playlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
    </ul>
</div>
<% end %>

<div id="error_explanation" class="alert alert-danger hidden" role="alert">
    <strong>Oh snap !</strong>
    <p><ul id="error_explanation_list"></ul></p>
</div>

<div class="row">
    <div class="input-group">
        <span class="input-group-addon">Name</span>
        <%= f.text_field :name, class: "form-control", placeholder: "My playlist", autofocus: true %>
    </div>
    <div class="input-group">
        <span class="input-group-addon">Description</span>
        <%= f.text_field :content, class: "form-control", placeholder: "Describe your playlist", autofocus: true %>
    </div>
</div>
<div class="row actions">
    <%= f.submit class: "btn btn-primary btn-lg" %>
</div>
<% end %>
